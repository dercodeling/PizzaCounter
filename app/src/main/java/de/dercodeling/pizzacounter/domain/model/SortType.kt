package de.dercodeling.pizzacounter.domain.model

import de.dercodeling.pizzacounter.ui.screens.home.BottomSheetOption

enum class SortType: BottomSheetOption {
    NAME,
    QUANTITY_ASC,
    QUANTITY_DESC
}